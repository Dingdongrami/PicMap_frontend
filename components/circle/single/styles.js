import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  mapContainer: {
    flex: 1,
    // flexDirection: 'row',
    width: "mapWidth",
    height: 221,
    // marginTop: 8
  },
})