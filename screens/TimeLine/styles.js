import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    // borderWidth: 0.5,
    borderColor: '#44403C',
    backgroundColor: 'white',
    paddingHorizontal: 8,
    marginTop: -8,
  },
});
