import { View, Text, SafeAreaView, StatusBar, Platform, StyleSheet, TouchableOpacity } from 'react-native';
import React, { useState } from 'react';
import { FontAwesome } from '@expo/vector-icons';

export const MyPage = () => {
  return (
      <View>
        <Text>MyPage</Text>
      </View>
  );
};
