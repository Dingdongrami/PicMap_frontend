import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    borderWidth: 1,
    marginTop: 50
  },
  map: {
    width: "100%",
    height: "100%"
  }
})